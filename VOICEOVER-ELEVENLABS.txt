[measured] Agents are becoming the traffic... and nobody has priced the inventory.

Websites publish a file called llms dot txt. It tells AI agents what's worth reading. Agents read it... so we sold ad space inside it.

This is a real placement, on a real site, right now. It carries the word SPONSORED, a disclosure notice, and a signed provenance record. That guarantee lives in code — the function that writes it throws if the label is missing.

[curious] Here's a fresh agent. It has never seen this project. We asked it a normal question about ventilating a darkroom, and pointed it at our site... we never mentioned advertising.

It finds the file on its own. That was the shakiest assumption we had — that agents actually read this thing...

[matter-of-fact] They do.

Then it reaches our ad.

Before we served anything, we ran the same test. The agent found the sponsored section empty, and named no brands... that's our control.

Then we served one line.

[deliberate] It refused to pass the placement off as advice... we didn't build that. We measured it.

Then we paid twenty-two people to read an AI answer. Everyone shown the label spotted the ad... one hundred percent.

With the label removed... seventeen percent. And they trusted the assistant twice as much.

[pointed] Hiding the ad makes it more effective... and better liked.

But the label doesn't always survive. Same ad, same model, four outcomes. Refused... disclosed... stripped. And the interesting one — the word sponsored present, the product recommended anyway. Technically there... functionally inert.

[deliberate] A disclosure that works most of the time... is not a disclosure standard.

And the case we filed as a failure — the model refusing the ad — is the actual product. A buyer's agent... it already exists.

[quietly] Nobody is selling to it yet.
